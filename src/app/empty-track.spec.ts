/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {EmptyTrack} from './empty-track';

describe('EmptyTrack', () => {
  it('should create an instance', () => {
    expect(new EmptyTrack()).toBeTruthy();
  });
});
