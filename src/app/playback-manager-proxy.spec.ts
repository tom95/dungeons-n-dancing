/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import {PlaybackManagerProxy} from './playback-manager-proxy';

describe('PlaybackManagerProxy', () => {
  it('should create an instance', () => {
    expect(new PlaybackManagerProxy()).toBeTruthy();
  });
});
