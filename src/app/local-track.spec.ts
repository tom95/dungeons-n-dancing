/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import {LocalTrack} from './local-track';

describe('LocalTrack', () => {
  it('should create an instance', () => {
    expect(new LocalTrack()).toBeTruthy();
  });
});
